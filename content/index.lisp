(defnode (index) +qua-hub-main-page+
  (:title "Qua: Ultralight Lisp for the Web (alpha)")
  (:header (image (:src "../img/lisp.png")))
  (:child
   (hyper '(intro))
   (hyper '(alpha))
   (hyper '(manual))
   (hyper '(usage))))
