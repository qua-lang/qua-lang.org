(defnode (index) +qua-hub-main-page+
  (:title "Qua: Ultralight Lisp for the Web (alpha)")
  (:child
   (hyper '(intro))
   (hyper '(tour))
   (hyper '(alpha))
   (hyper '(manual))
   (hyper '(usage))))
